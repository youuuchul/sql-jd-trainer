# AI 작업 기록 (Codex)
- 일시: 2026-02-13
- 에이전트: Codex (GPT-5 기반)
- 적용 브랜치: codex/feat-kor-ui-history
- 사용/대상 모델: Google Gemini 2.5 Flash Preview (프론트엔드 호출), 오프라인 mock fallback 병행
- 목표: 한국어 UI/문제화, API 상태 배지 추가, 사용 이력 남기기

## 변경사항
1) 프롬프트 한글화
   - 문제/힌트/피드백을 한국어로 생성하도록 시스템 프롬프트 수정
   - 피드백 프롬프트를 한국어 지시 + JSON 응답 강제
2) UI 한글화
   - 버튼/로딩/배지/피드백 타이틀 등 주요 문구 한국어로 교체
   - 난이도 뱃지 출력 시 한글 레이블 변환(주니어/미들/시니어)
   - API 상태 배지(모델/키 존재/호출 횟수) Setup & Workspace 헤더에 표시
3) 안정성/가독성
   - Gemini 키 누락/호출 실패 메시지 한국어화
   - 피드백 실패 메시지 한국어화
4) 기록/무시 파일
   - `.gitignore`에 `.env.local`, `.npmrc` 추가
   - `ai_history/` 폴더 생성 및 본 기록 작성

## 사용한 프롬프트/지시 요약
- SYSTEM_PROMPT_ANALYSIS: "문제/힌트는 한국어, 스키마는 영어" 규칙 추가
- SYSTEM_PROMPT_FEEDBACK: 한국어 피드백, JSON 포맷 강제
- 피드백 호출 메시지: 한국어 컨텍스트 + JSON 형식 지시

## TODO
- 프록시 구성(서버 측 키 보호) 및 비용/토큰 사용량 표시 연동
- 난이도 값(Junior/Middle/Senior)을 모델 출력 단계에서부터 한글로 강제하도록 추가 지시
- 세션 목록 삭제/정렬, 세션 내 제출 히스토리 저장
- UI 남은 영문(테이블 헤더 등) 추가 점검

## 추가 작업 (문제 한국어 보정)
- 문제 텍스트가 영어로 생성되는 경우를 대비해 한국어 보정 단계 추가
  - problems 배열 내 title/description/hint/expectedLogic/difficulty가 한글을 포함하지 않으면
    `SYSTEM_PROMPT_TRANSLATE`로 재요청하여 한국어로 변환
- SYSTEM_PROMPT_ANALYSIS를 전면 한국어화 및 "출력은 JSON 하나만" 강제
- 난이도 표시가 대문자/소문자 영어여도 한글로 매핑되도록 보강
- 헤더 타이틀 일부 한국어화(SQL 트레이너)

## 추가 작업 (영문 문제 자동 한국어화 보강)
- 한국어 여부 판별 로직 확대: domain/keywords/problems 전체 검사
- 영문이면 1차 전체 로컬라이즈(SYSTEM_PROMPT_LOCALIZE) 요청
- 그래도 영문이면 problems만 2차 보정(SYSTEM_PROMPT_TRANSLATE)
- 저장된 세션 로드 시에도 한글 보정 수행하고 로컬스토리지 업데이트

## 추가 작업 (SQL 에디터 개선/주석/datediff)
- CodeMirror 6를 CDN으로 로드해 SQL 에디터 개선(라인 번호, 하이라이트, 주석 토글 Mod+/)
- SQL 문법은 MySQL dialect로 지정(DATEDIFF 하이라이트 포함)
- AlaSQL에 DATEDIFF 함수 추가(2-인자/3-인자 방식 지원)
- 쿼리 실행 전 전처리로 #, // 주석 라인을 -- 로 변환

## 추가 작업 (MySQL 실제 엔진 연결)
- Express + mysql2 기반 API 서버 추가 (`server/index.js`)
- `/api/health`, `/api/init`, `/api/query` 엔드포인트 구현
- 스키마/데이터를 MySQL에 반영하고 SELECT/CTE 쿼리 실행
- 컬럼 타입 자동 추론(BOOL/INT/DECIMAL/DATE/DATETIME/TEXT)
- 프론트에서 백엔드 연결 감지 및 MySQL 모드 실행
- Vite proxy(`/api` → `http://localhost:8787`) 추가
- README에 MySQL 모드 사용법 추가

## 추가 작업 (MySQL 모드 강화/쿼리 상태 유지)
- 세션별 마지막 쿼리 저장(localStorage) 및 재진입 시 복원
- MySQL 컬럼 타입 추론 개선(컬럼명 힌트 + 데이터 기반)
- MySQL 세션 SQL 모드 적용(STRICT_TRANS_TABLES 기본값)
- server/.env.example에 MYSQL_SQL_MODE 추가

## 추가 작업 (npm 의존성 없이 MySQL CLI 기반 서버로 전환)
- npm 레지스트리 DNS 불가로 인해 Express/mysql2 제거
- Node 내장 http + mysql CLI 사용 방식으로 서버 구현
- server/.env 수동 로딩 기능 추가
- /api/health, /api/init, /api/query 그대로 유지
- MySQL CLI 출력(TSV) 파싱하여 rows 반환

## 추가 작업 (DB 자동 생성)
- /api/health 호출 시 데이터베이스가 없으면 자동으로 CREATE DATABASE 수행
- runMysql 함수에 useDb 옵션 추가하여 DB 미생성 시에도 동작

## 추가 작업 (SQL 에디터 포커스/입력 보강)
- CodeMirror 초기화 후 focus 강제
- SqlEditor useEffect 의존성 보강

## 추가 작업 (CodeMirror 스타일 주입)
- CodeMirror base CSS를 CDN으로 주입해 에디터 내용이 보이도록 수정
- 에디터 테마에 텍스트/커서 색상 명시

## 추가 작업 (CodeMirror CDN 교체)
- cdn.jsdelivr ESM 모듈에서 bare import 오류 발생
- esm.sh 번들 URL로 교체해 종속성까지 단일 URL에서 해결
- CodeMirror CSS는 unpkg로 전환

## 추가 작업 (CodeMirror 5로 전환)
- CodeMirror 6 ESM 로딩 실패로 인해 CodeMirror 5 CDN 방식으로 교체
- textarea 기반 fromTextArea 사용, SQL 모드 및 주석 토글(Cmd/Ctrl + /) 활성화
- CodeMirror CSS/스크립트 jsdelivr CDN 로드

## 추가 작업 (커서 점프/줄번호 이슈 완화)
- CodeMirror 초기화 useEffect 의존성에서 value 제거 → 입력마다 재초기화 방지
- textarea readOnly 제거 (CodeMirror가 숨김 처리)

## 백로그 정리
- 스키마 컬럼 표시 개선(가독성, 구분자/줄바꿈)
- 문제 전환 시 SQL 에디터 내용 유지/초기화 동작 정의
- 문제별 기본 쿼리 템플릿 제공
- MySQL 모드 에러/결과 메시지 표준화

## Skills 추가
- skills/sql-jd-trainer-troubleshooting/SKILL.md
- skills/sql-jd-trainer-mysql-mode/SKILL.md

## 추가 작업 (AlaSQL DB 중복 오류 무시)
- CREATE DATABASE IF NOT EXISTS 호출 시 중복 오류가 발생해도 무시 처리
